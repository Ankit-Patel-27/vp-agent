[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip install --break-system-packages gunicorn flask flask-cors requests feedparser yfinance"
]

[start]
cmd = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
